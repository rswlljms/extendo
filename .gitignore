node_modules/
dist/
host/core/target/
*.log
.DS_Store
# android local config + build caches (AGENTS.md: never commit local state)
local.properties
client-android/.gradle/
client-android/build/
client-android/app/build/
client-android/.idea/
# secrets / local state — never commit
.env
.env.local
*.pem
*.key
*.p12
*license*.json
extendo.config.json
# driver binaries — link releases only (AGENTS.md §7.2)
driver/*.sys
driver/*.cat
driver/*.inf
driver/bin/
# local diag bundles
extendo-diag-*/
# installer output — never commit built packages (AGENTS.md §7.2)
installer/out/
*.msi
# WiX extension cache (downloaded binaries; restored by build.ps1)
installer/.wix/
